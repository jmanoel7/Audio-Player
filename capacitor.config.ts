import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.jmanoel7.AudioPlayer',
  appName: 'Audio Player',
  webDir: 'dist/music-list',
  bundledWebRuntime: true
};

export default config;
