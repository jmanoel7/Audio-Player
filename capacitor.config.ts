import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.jmanoel7.AudioPlayer',
  appName: 'iTunes Audio Player',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
